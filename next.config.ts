import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
  basePath: '/protein-mix-calculator',
  assetPrefix: '/protein-mix-calculator',
  /* config options here */
};

export default nextConfig;
